syntax = 'proto3';

package beget.site.v1.structures;

message Site {
    uint64 id = 1;
    string document_root = 2; //user home dir based path
    repeated string linked_fqdn = 3;
    bool is_frozen = 4;
    bool is_secure = 5;
    string name = 6;
}